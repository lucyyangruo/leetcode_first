数组
特点：
1. 存储在连续内存空间上
2. 相同类型数据的集合
存储内容：
存储具体数据和下标，下标从0开始。
注意：
1. 存储空间地址是连续
2. 下标从0
数组的操作：
删除/增加元素则需要移动后面的元素地址。
数组元素只能覆盖，不能删除。
二维数组的存储内容：
依据编程语言而不同，c++是连续分布的，java中是乱序的。
也就是c++依照二维数组由行到列的方式排列成一条线，内存地址是连续的，可读的。
java是将二维数组中每行的数据存储在一个地址中，下一行在上一行的地址连续下。
